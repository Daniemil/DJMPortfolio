version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - resume_current.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
